/*

export const uploadImage = (data) => http('POST', '/api/common/image/upload', data, {
  headers: {
    'Content-Type': 'multipart/form-data'
  }
})

// 获取商品规格列表
export const getProductSpecifications = (params) => http('GET', '/api/product/specification', params)

*/
